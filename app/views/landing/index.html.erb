<div class="row">
  <h1>Landing#index</h1>
  <p>This is from desktop page view.</p>
</div>
<div class="row">
  <h3>List of users</h3>
 <% @users.each do |user| %>
  <%= user.email %><br>
 <% end  %>
 <h3>Current User</h3>
 <%= current_user.email %>
 <% %>
 <br>
 <% if current_user.profile.blank? %>
    we need a profile
 <% end %>

</div>
